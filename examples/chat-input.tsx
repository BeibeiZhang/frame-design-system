import { ChatInput } from '../components/chat-input';
import '../styles/frame.css';

export default function Example() {
  return <div className="frame-root"><ChatInput features={{}} onSend={() => {}} /></div>;
}
